#include "types.h"

/** @file types.c
 * @brief Support code for type definitions
 *
 * This (mostly) empty file exists in order to include the
 * associated header file, and nothing else, to assure that
 * it is a complete and self contained compilation unit.
 */
